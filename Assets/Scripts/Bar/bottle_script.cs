using UnityEngine;

public class bottle_script : MonoBehaviour
{
    public bottle_SO bottle_data;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
